# Dependencies for local development on Windows or macOS.
#
# Deliberately smaller than requirements.txt:
#   - psycopg is omitted because local development runs on SQLite, and the
#     binary wheel is not always available for the newest Python on Windows.
#   - gunicorn is omitted because it cannot run on Windows; use runserver.
#
# Install with:
#     python -m venv .venv
#     .venv\Scripts\activate
#     pip install -r requirements-local.txt

Django==6.0.3
django-environ==0.13.0
whitenoise==6.12.0
celery[redis]==5.6.2
httpx==0.28.1
openai==2.26.0
